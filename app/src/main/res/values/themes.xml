<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjectY" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/DungeonTowerHex1</item>
        <item name="colorPrimaryDark">@color/DungeonTowerHex4</item>
        <item name="colorAccent">@color/white</item>
    </style>
    <style name="Theme.ProjectY.Fullscreen" parent="Theme.ProjectY">
        <item name="android:actionBarStyle">@style/Widget.Theme.ProjectY.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.ProjectY.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>